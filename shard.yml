name: engine-rest-api
version: 1.0.0

dependencies:
  action-controller:
    github: spider-gazelle/action-controller
    version: "~> 1.3"

  # For rethink models
  engine-models:
    github: aca-labs/crystal-engine-models

  # For redis access libraries
  engine-driver:
    github: aca-labs/crystal-engine-driver

  # Microlibrary to perform ping requests
  pinger:
    github: spider-gazelle/pinger

  # Data validation library
  active-model:
    github: spider-gazelle/active-model

development_dependencies:
  ameba:
    github: veelenga/ameba

  faker:
    github: askn/faker

# compile target
targets:
  app:
    main: src/app.cr
