name: engine-rest-api
version: 1.0.0

dependencies:
  action-controller:
    github: spider-gazelle/action-controller
    version: "~> 1.3"

  # For rethink models
  engine-models:
    #github: aca-labs/crystal-engine-models
    path: /home/caspian/work/engine/models

  # For redis access libraries
  engine-driver:
    github: aca-labs/crystal-engine-driver

  # https://github.com/jeromegn/kilt
  # Generic template interface for Crystal
  kilt:
    github: jeromegn/kilt

development_dependencies:
  ameba:
    github: veelenga/ameba

# compile target
targets:
  app:
    main: src/app.cr
